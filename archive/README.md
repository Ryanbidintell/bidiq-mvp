# archive/

Superseded / one-shot files kept for reference only. **Nothing here is live.**

- **Product Bibles v1_4 – v2_0** — superseded by `BidIntell_Product_Bible_v2_1.md` in the repo root (the current one).
- **MORNING_BRIEFING.md, OVERNIGHT_FIXES_SUMMARY.md** — dated session notes.
- **One-shot migration/fix scripts** (`create-*-migration.js`, `execute-*.js`, `fix-*.js`, `generate-*-migration.js`) — one-time scripts already run against the DB. The live migration runner is `run-migrations.js` in the repo root (`npm run migrate`).

Archived 2026-06-02 during a docs/cleanup pass. Safe to delete entirely if you don't want the history in-tree (git keeps it regardless).
